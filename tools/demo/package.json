{
  "name": "@slipstream/demo",
  "version": "0.0.0",
  "private": true,
  "description": "Headed Playwright script that records the canonical README demo flow.",
  "scripts": {
    "install:chromium": "playwright install chromium",
    "record": "tsx record-demo.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "echo 'lint: demo (no rules wired yet)'",
    "test": "echo 'test: demo (this IS a test of the live demo)'",
    "build": "echo 'build: demo (nothing to build)'"
  },
  "devDependencies": {
    "@types/node": "22.10.2",
    "playwright": "1.49.1",
    "tsx": "4.19.2",
    "typescript": "5.6.3"
  }
}
